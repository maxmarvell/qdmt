name: qdmt
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - numpy>=1.24.0
  - scipy>=1.10.0
  - matplotlib>=3.7.0
  - pip
  - pip:
      - ncon>=2.0.0
      - opt_einsum>=3.3.0
      - pytest>=7.0.0
      - pytest-cov>=4.0.0
      - pytest-benchmark>=4.0.0
      - ruff>=0.1.0
      - mypy>=1.0.0
      - jupyter>=1.0.0
      - ipython>=8.0.0
